[server]
http_addr =
http_port = ${GF_SERVER_HTTP_PORT}
domain = ${GF_SERVER_DOMAIN}
cert_file = /certs/local-cert.pem
cert_key = /certs/local-key.pem
protocol = https
[security]
admin_user = ${GF_USER}
admin_password = ${GF_PASSWORD}
